import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color(0xFF007BFF);  // Blue from UI
  static const background = Colors.white;
  static const Color primaryColor = Color(0xFF1976D2); // Define primaryColor

// Add more
}